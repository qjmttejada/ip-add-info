name: Build IP Add Info
run-name: ${{ github.actor }} is building IP Add Info 🚀

on: push

jobs:
  runscript:
    name: BuildIpAddInfo
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        run: bash ./ipAddInfo.sh
      
